IMAGE NOTE
---------
Project visuals are intentionally lightweight placeholder SVGs so the portfolio opens offline with no broken image links. Replace them with real screenshots in this folder and update the matching <img src="..."> in index.html.

The uploaded pic.html referenced a missing companion pic_files/ directory, so no usable portrait asset was available. The hero currently uses an animated CSS/SVG-style EA avatar instead.


PROJECT SCREENSHOTS
--------------------
Gallery images live in assets/projects/. When available, add mood-1.png and
game-1.png there; their project pages contain non-breaking placeholders for them.
